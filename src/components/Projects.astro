---
import GitHub from "./icons/GitHub.astro"
import NextJS from "./icons/NextJS.astro"
import Python from "./icons/Python.astro"
import R from "./icons/R.astro"
import Tensor from "./icons/Tensor.astro"
import Link from "./icons/Link.astro"
import LinkButton from "./LinkButton.astro"

const TAGS = {
  AWS: {
    name: "Amazon Web Services",
    class: "bg-[#2FD182] text-white",
    icon: NextJS,
  },
  PYTHON: {
    name: "Python",
    class: "bg-[#2FD182] text-white",
    icon: Python,
  },
  R: {
    name: "R Language",
    class: "bg-[#2FD182] text-white",
    icon: R,
  },
  TENSOR: {
    name: "TensorFlow",
    class: "bg-[#d62828] text-white",
    icon: Tensor,
  },
}
const PROJECTS = [
  {
    title: "Detección De Rostros utilizando OpenCV y una Red Neuronal",
    description:
      "Detección De Rostros utilizando OpenCV y una red neuronal entrenada con imágenes.",
    link: "https://github.com/carlos-ruiz10/DeteccionDeRostros/tree/main",
    github: "https://github.com/carlos-ruiz10/DeteccionDeRostros/tree/main",
    image: "/projects/img.jpeg",
    tags: [TAGS.AWS, TAGS.PYTHON, TAGS.TENSOR],
  },
  {
    title: "Análisis Fraude con tarjetas de crédito",
    description:
      "Los pagos digitales están evolucionando, pero también lo hacen los ciberdelincuentes. En el mundo digital actual, donde se realizan billones de transacciones con tarjeta por día, la detección del fraude es un desafío.",
    link: "https://www.kaggle.com/code/carlosruizcc/analysis-of-the-dataset-knn-dt-msv-rf",
    image: "https://psn.si/wp-content/uploads/2023/12/fraude.jpeg",
    tags: [TAGS.R, TAGS.PYTHON],
  },
]
---

<div class="flex flex-col gap-y-16">
  {
    PROJECTS.map(({ image, title, description, tags, link, github }) => (
    <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
  <div class="w-full md:w-1/2">
    <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
      <img alt="description" class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105" loading="lazy" src={image} />
    </div>
  </div>

  <div class="w-full md:w-1/2 md:max-w-lg">
    <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">
      {title}
    </h3>
    <div class="flex flex-wrap mt-2">
      <ul class="flex flex-row mb-2 gap-x-2">
          {tags.map((tag) => (
            <li>
              <span
                class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2 `}
              >
                <tag.icon class="size-4" />
                {tag.name}
              </span>
            </li>
          ))}
        </ul>

      <div class="mt-2 text-gray-700 dark:text-gray-400">{description}</div>
      <footer class="flex items-end justify-start mt-4 gap-x-4">
          {github && (
            <LinkButton href={github}>
              <GitHub class="size-6" />
              Code
              
            </LinkButton>
          )}
          {link && (
            <LinkButton href={link}>
              <Link class="size-4" />
              Preview
            </LinkButton>
          )}
        </footer>
    </div>
  </div>
</article>
    ))
  }
</div>



